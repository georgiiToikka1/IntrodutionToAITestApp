# Project Package Configuration

This document specifies the project metadata, scripts, and dependencies.

```json
{
  "name": "firebase-auth-app",
  "version": "1.0.0",
  "type": "module",
  "description": "Firebase authentication utility with email and password sign-in",
  "main": "firebase.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase": "^11.0.0"
  }
}
```

## Dependency Information

- **Package**: `firebase`
- **Version**: `^11.0.0`
- **Module System**: ES Modules (`"type": "module"`)

## Installation

```bash
npm install firebase
```
